import { z } from "zod";
import { assertSameOrigin, requireRetailPermission } from "@/src/lib/retail/admin-auth";
import { archiveAdminProduct, changeProductPrice, priceDto, productUpdateDto, updateAdminProduct } from "@/src/lib/retail/operations";
export const runtime="nodejs"; export const dynamic="force-dynamic";
const fail=(e:unknown,s=400)=>Response.json({ok:false,error:e instanceof Error?e.message:"invalid_request"},{status:s,headers:{"cache-control":"no-store"}});
type ParamsContext={params:Promise<{id:string}>};
export async function PATCH(request:Request,context:ParamsContext){try{await requireRetailPermission("products:write");await assertSameOrigin();const {id}=await context.params;const body=await request.json();if(body.action==='price')return Response.json({ok:true,result:await changeProductPrice(id,priceDto.parse(body))},{headers:{"cache-control":"no-store"}});return Response.json({ok:true,product:await updateAdminProduct(id,productUpdateDto.parse(body))},{headers:{"cache-control":"no-store"}})}catch(e){const message=e instanceof Error?e.message:"invalid_request";return fail(new Error(message==="unauthorized"||message==="forbidden"?message:"invalid_request"),message==="unauthorized"?401:message==="forbidden"?403:400)}}
export async function DELETE(request:Request,context:ParamsContext){try{await requireRetailPermission("products:write");await assertSameOrigin();const {id}=await context.params;const body=z.object({idempotencyKey:z.string().uuid()}).strict().parse(await request.json());return Response.json({ok:true,product:await archiveAdminProduct(z.string().uuid().parse(id),body.idempotencyKey)},{headers:{"cache-control":"no-store"}})}catch(e){const message=e instanceof Error?e.message:"invalid_request";return fail(new Error(message==="unauthorized"||message==="forbidden"?message:"invalid_request"),message==="unauthorized"?401:message==="forbidden"?403:400)}}
